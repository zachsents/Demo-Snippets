#bold,white and finally, we'll make it move over on hover
#bold,yellow move underneath .overlay
#blue now add wrapper background back in
.wrapper:hover .overlay {
    transform: translate(100px, 10px);
}